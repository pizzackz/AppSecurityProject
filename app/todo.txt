General:
    Write handlers for any failed checks for jwt_required & csrf
    Write general session checker to check for required keys

signup_auth_bp:
    Track otp attempts in session
    Use api to help in getting address & postal code in extra_info (Align with Ervyn)

recovery_auth_bp:
    Write session stage checker
    Write jwt value checkers
    Write route controllers (
        account_recovery - base, send_otp, verify_email,
        recovery_options, send_username, set_new_password
    ) --> Routes subject to change

login_auth_bp:
    Write session stage checker
    Write route controllers (login, send_otp, verify_email)
    Use Google Sign-in API
